apiVersion: v1
kind: Service
metadata:
  name: {{ include "dns-api.fullname" . }}
  labels:
    {{- include "dns-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dns-api.selectorLabels" . | nindent 4 }}
